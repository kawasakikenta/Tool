# エージェントの役割分担

このリポジトリでは、親エージェントが計画・作業の割り振り・成果の統合・最終確認を担当する。
親のモデルは利用者が起動時に選んだものを維持し、実行担当の子エージェントを次の設定にそろえる。

| 利用する環境 | 実行担当 | 推論強度 | 設定ファイル |
| --- | --- | --- | --- |
| Codex / GPT | `gpt-luna` | `max` | `.codex/config.toml` の `[agents]` |
| Claude Code | `repo-executor`（`sonnet`（最新版エイリアス。2026年9月時点で Sonnet 5）） | `high` | `.claude/agents/repo-executor.md` |

## 作業の進め方

1. 親は既存の `AGENTS.md`・`CLAUDE.md` と対象ディレクトリのルールを確認し、目的・変更範囲・検証方法を整理する。
2. 実装・調査・検証を、独立して完了できる単位で子エージェントに委譲する。短い確認や分割する利点がない作業は親が行ってよい。
3. 委譲時には、目的、必要な背景、担当ファイル、変更してよい範囲、完了条件、必要な検証を具体的に伝える。
4. 独立した作業は並列に進める。同じファイルの編集や前の結果が必要な作業は順番に進める。
5. 親は成果を読み、差分と検証結果を確認して統合する。失敗や未検証事項も最終報告に含める。

## 実行担当の選び方

- Codex では子エージェントのモデルを `gpt-luna`、推論強度を `max` にする。明示的な起動指定が必要な場合も、この組み合わせを指定する。
- Claude Code では調査・実装・検証の委譲先に `repo-executor` を指定する。このカスタムエージェントは Sonnet 5（最新版） / high で実行する。
- 指定モデルを利用できない場合は、別モデルへ黙って切り替えず、利用できなかった設定と理由を親へ報告する。
- 子エージェントは依頼された範囲に集中し、追加の割り振りが必要なら親へ返す。

## 変更と検証

- 既存の未コミット変更を保持し、自分の担当外の変更を取り消さない。
- 生成物、改行コード、ビルド方法、必要なテストは、このリポジトリの個別ルールに従う。
- 子エージェントは、変更内容・変更ファイル・実行した検証と結果・残った問題を親へ返す。
- モデル設定のために、承認・権限・サンドボックスや認証情報の設定を変更しない。

## 設定の読み込み

設定はリポジトリ内に置いてある。反映を確認するときは、このリポジトリを開いた新しいセッションを開始する。
Codex のプロジェクト設定は、そのプロジェクトを信頼済みとして扱う場合に読み込まれる。
Claude Code の Sonnet 5（最新版） / high は `repo-executor` を起動したときに適用され、親セッションのモデル指定とは独立している。

仕様の参照先:

- [Codex のサブエージェント設定](https://learn.chatgpt.com/docs/agent-configuration/subagents)
- [Codex のプロジェクト設定](https://learn.chatgpt.com/docs/config-file/config-basic)
- [Claude Code のカスタムサブエージェント](https://code.claude.com/docs/en/sub-agents)
